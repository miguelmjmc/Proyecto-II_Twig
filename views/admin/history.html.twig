{% extends "base.html.twig" %}

{% block main %}

    <!-- Main content -->
    <section class="content" xmlns="http://www.w3.org/1999/html">
        <div class="row">
            <div class="col-xs-12">
                <div class="box">
                    <div class="box-header">
                        <h3 class="box-title">Historial</h3>
                    </div>
                    <!-- /.box-header -->
                    <div class="box-body">
                        <table id="maintable" class="table table-bordered table-hover">
                            <thead>
                            <tr>
                                <th class="th-3">Usuario</th>
                                <th class="th-3">Operación</th>
                                <th class="th-3">Fecha</th>
                            </tr>
                            </thead>
                            <tbody>
                            {% for history in array.history %}
                                <tr>
                                    <td class="td-3">{{ history.email }}</td>
                                    <td class="td-3">{{ history.action }}</td>
                                    <td class="td-3">{{ history.date }}</td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                        <!-- / .table -->
                    </div>
                    <!-- /.box-body -->
                </div>
                <!-- /.box -->
            </div>
            <!-- /.col -->
        </div>
        <!-- /.row -->
    </section>

{% endblock %}